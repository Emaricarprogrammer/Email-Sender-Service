name: CI/CD PIPELINE
on:
  push:
    branches:
      - main
jobs:
  build:
  runs-on: ubuntu-latest
  steps:
    - name: checkout the code
      uses: actions/checkout@v4
    
    - name: set up node js
      uses: actions/setup/node@v4
      with:
        node-version: "22"
    - name: install dependencies
      run: npm install
deploy:
  runs-on: ubuntu-latest
  needs: build
  steps:
    name: deploy to production
    run: echo "inital pipeline"
    
